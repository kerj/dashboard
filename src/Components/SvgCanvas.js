import React, { Component } from 'react';
import * as d3 from 'd3';

export default class SvgCanvas extends Component {

    componentDidMount() {
        //Plan will be to make an svg component to stay loaded on page and allow component to
        //rotate in and animate based on lifecycle functions. 
    }

    render() {
        return (
            <div>

            </div>
        )
    }
}
